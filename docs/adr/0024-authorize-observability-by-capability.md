# Authorize observability by capability

Full Agent Trace and Evaluation surfaces are developer capabilities, while users receive only their own Decision Explanations, citations, processing status, and approval history. Developer access is redacted by default; sensitive payload access, when supported, is time-limited and audited, and the system does not collect or expose hidden model reasoning as an observability feature.
