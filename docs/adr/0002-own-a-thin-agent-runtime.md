# Own a thin Agent Runtime

GetOffers will implement a small domain-oriented Agent Runtime instead of making DeepSeek Harness, nanobot, or LangGraph the product platform. The runtime will expose stable run, event, tool, policy, approval, checkpoint, cancellation, and replay contracts; external frameworks may later be compared behind an adapter without owning Job Search, Knowledge Retrieval, tenant permissions, or evaluation data.
