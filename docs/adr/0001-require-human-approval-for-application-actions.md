# Require human approval for application actions

GetOffers may automate deterministic collection, parsing, and indexing, but the Career Agent will not submit applications autonomously. Creating an Application Plan requires explicit user confirmation, and recruitment-site autofill remains a user-initiated browser action so that external side effects and uncertain personal data stay under human control.
