# Run versioned paired evaluations

Every evaluation pins dataset, corpus, user state, parser, segmentation, retrieval, model, prompt, toolset, Runtime, pricing, and grader identities. Baseline and Challenger use paired Evaluation Cases; deterministic graders establish constraints, retrieval, citations, tools, approval, usage, and latency before model-based graders add human-calibrated subjective judgments.
