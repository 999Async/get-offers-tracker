# Phase 2：知识库本地工程验证

日期：2026-09-06。结论：本地工程基线可运行，完整 Phase 2 / 生产发布门禁尚未通过。

## 交付内容

- 工作台侧栏“个人知识库”内容区及认证 Product API：材料上传、处理状态、历史版本、原文件、原文引用、事实确认/修正/拒绝、删除与重试。
- Canonical Document、结构化 Evidence Unit、不可变 source/canonical 对象，SQLite 本地事实存储、对应 D1 表结构迁移与可选 R2 适配器。
- 真实 Docling PDF/DOCX 适配；Markdown/TXT 确定性解析。保留标题、列表、表格或问答结构与来源位置。
- 私有 Qdrant BM25、BGE dense、RRF hybrid、BGE rerank；过滤、引用校验、邻近证据扩展、去重与上下文预算。
- 带修订检查的事实审核、新版本原子切换、跨存储删除与手动 reconciliation。

运行和设计边界见 [知识库指南](../../services/agent/KNOWLEDGE.md)。本轮没有修改 Phase 1 审核标签或将待确认用例纳入指标。

## 已验证

| 检查 | 结果 | 范围 |
|---|---|---|
| Python 全量回归 | 142 passed，3 skipped | 跳过真实模型/文档 opt-in 集成测试，分别说明 |
| 真实 Docling 集成 | 2 passed | 中文 DOCX 标题/表格，原生文本 PDF 页码/bbox/引用回查 |
| 网页构建与 Node 测试 | 16 passed | 包含认证、身份注入拒绝、请求大小、令牌绑定及重定向拒绝 |
| Python Ruff | 通过 | 全部 Python 文件 |
| 知识库页面/API ESLint | 通过 | 新增目录；未把首页既有 lint 问题视为本轮已修复 |
| D1/SQLite 列结构一致性 | 通过 | 本地执行新增迁移并比较列、类型、非空与主键 |
| 浏览器本地流程 | 通过已测操作 | 中文上传、检索、原文定位、事实确认，390 px 无横向溢出 |
| 实际 Product API | 通过 | 事实确认读回、版本替换、旧引用拒绝、检索、删除及零材料/零事实读回 |
| 过期页面结果回归 | 通过 | 后端删除材料后刷新，页面清除已删除版本的证据 |

浏览器使用仅监听本机的临时测试网关注入固定合成身份，实际调用现有认证逻辑、Cloudflare 开发运行时、Product API 和 Python 服务；不是线上用户认证验证。上传与审核通过页面操作，版本替换/删除通过实际 Product API 操作，最后在页面刷新确认结果。测试材料不含个人信息。

联调实际发现并修复两项问题：Cloudflare fetch 不接受 `redirect: error`，改用 manual 并拒绝 3xx；页面刷新原先只刷新材料和事实，现同时淘汰非活动版本的已展示证据与引用预览。

删除测试覆盖 Qdrant 清理、对象清理、数据库衍生事实删除、剩余材料索引重建的失败重试。重新查询被删文档、原文件及关联事实均不可用，历史索引也无对应文档点；其他租户仍可检索。未验证外部备份、生产 R2 或未来 Run Trace 的删除覆盖。

## 合成检索评测

使用 [knowledge-v1](../../datasets/evals/knowledge-v1/README.md) 的 12 份 Markdown/TXT、24 条问题，预期证据为显式标记原文片段。问题与原文词汇高度重叠，难度低；人工审核用例为 0。四组都使用同一 corpus、活动版本、范围和标签；神经组使用真实缓存 BGE 模型、MPS 执行，并以离线环境复跑。

| 模式 | Recall@10 | nDCG@10 | p50 ms | p95 ms |
|---|---:|---:|---:|---:|
| lexical | 1.0000 | 0.9846 | 1.60 | 2.23 |
| dense | 1.0000 | 1.0000 | 26.54 | 36.51 |
| hybrid | 1.0000 | 0.9846 | 29.04 | 34.82 |
| hybrid-rerank | 1.0000 | 1.0000 | 205.41 | 264.47 |

所有返回引用均重新验证原文区间；12 份文档重复解析一致，预设结构与原文保留检查通过。PDF/DOCX 独立集成用例不计入 12 份数据。延迟为本地小语料查询端测量，不包含模型加载或全量索引构建；不能外推生产 SLA，也不能用本轮满分支持模型质量提升结论。

机器可读报告保存 dataset/config/code/lock hash、模型身份、逐条排名、引用与延迟：

- [仅词法报告](phase2-knowledge-lexical.json)
- [真实模型四组报告](phase2-knowledge-neural.json)

当前 code hash：`daa03f490c490afbb5fe9d63487716279b1d61ce94683ea4d8ae23f37a48633a`。此哈希覆盖 knowledge Python 模块；不代表整个仓库或网页源码版本。

## 尚未满足的阶段门禁

1. 收集并人工核验许可使用的 40–60 份 Parsing Gold 文档、约 100 条检索问题，覆盖简历多栏、跨页表格、问答、损坏/加密/扫描材料；当前不把这些质量要求标为通过。
2. 当前 PDF 不启用 OCR 和表格结构模型。复杂 PDF、生成式事实抽取、来源 PDF 高亮阅读器和答案忠实度评测未实现。
3. 远程 D1 事务适配、真实 R2/Qdrant 服务部署、生产独立解析隔离、总租户配额、调度删除 reconciliation 及备份清理未验证。
4. 确认事实已可通过 confirmed_only 获取，但尚未接入岗位硬条件或 Career Agent / Run Trace；这部分需要后续完整工作流验收。

原有正式 Phase 2 exit criteria 保持不变。本轮交付为可复查的本地实现与合成工程证据，不是正式发布批准。
